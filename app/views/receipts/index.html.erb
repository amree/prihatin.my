<section class="container grid-960 site-content">
  <div class="columns">
    <% if @payment.success? %>
      <%= render "success",
        payment: @payment,
        preview: @preview
      %>
    <% else %>
      <%= render "failed",
        payment: @payment
      %>
    <% end %>
  </div>
</section>
